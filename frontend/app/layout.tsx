import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Enterprise KB Agent — Multi-source knowledge assistant",
  description:
    "Ask one question, get one answer grounded in Slack, Notion and Google Drive. Multi-agent routing, hybrid retrieval, streamed responses and voice I/O.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
